# Frontend API base URL. Set this when the app and API run on different hosts/ports
# (e.g. on an Ubuntu VM without Nginx: use the backend URL so the browser can reach it).
#
# Examples:
#   Same machine, backend on port 3000:  http://localhost:3000/api
#   VM with hostname "hygieia":           http://hygieia:3000/api
#   VM by IP:                             http://192.168.1.100:3000/api
#
# Leave unset if the frontend is served behind Nginx that proxies /api to the backend
# (then the app uses relative /api).
# VITE_API_URL=http://your-server:3000/api
